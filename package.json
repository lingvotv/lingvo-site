{
  "lingvo-site": "0.1.0",
  "dependencies": {
    "smoothscroll": "^0.2.2",
    "social-share-kit": "^1.0.8"
  },
  "devDependencies": {
    "node-static": "^0.7.7",
    "localtunnel": "^1.8.1"
  },
  "scripts": {
    "start": "npm run static & npm run tunnel:site",
    "static": "static .  --port 8081",
    "tunnel:site": "lt -s lingvo -p 8081"
  }
}
